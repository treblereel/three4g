package org.treblereel.gwt.three4g.geometries;

import jsinterop.annotations.JsType;
import org.treblereel.j2cl.processors.annotations.ES6Module;

@ES6Module
@JsType(isNative = true, namespace = "org.treblereel.gwt.three4g.geometries", name = "BoxGeometry")
public class BoxBufferGeometry {

    public BoxBufferGeometry(float width, float height, float depth) {
    }
    public BoxBufferGeometry(float width, float height, float depth, int widthSegments, int heightSegments, int depthSegments) {
    }
}
